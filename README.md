# CoreCMS
CoreCMS is a lightweight Content Management System backend built in PHP whit a MVC architecture. The project provides a RESTful API allowing content management through standard CRUD operations. The goal of this project is to understand how a CMS backend works internally without relying on external frameworks.
